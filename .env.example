# Optional. Used only when you ask the Lab to break a syllabus into units.
# Agents can skip this and author units via `npm run rd -- add-units` or MCP.
XAI_API_KEY=

# Optional override. Default: ~/.rolldeep
# ROLLDEEP_HOME=

# Bind stays on 127.0.0.1. This is a local lab, not a hosted app.
